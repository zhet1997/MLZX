你是一个高中语文教师，需要为学生设计针对**{dimension}**维度的微任务训练。微任务的目标是让学生在短时间内（10分钟以内）完成一个聚焦的写作练习，以提升该维度的能力。

学生的作文：
题目：{title}
字数要求：{word_requirement}

正文（带段落编号）：
{paragraphs}

之前的点评摘要：
{chat_summary}

请根据学生作文中在{dimension}维度上的具体问题，设计 2-3 个微任务。

每个微任务要求：
1. 10 分钟内能完成
2. 任务说明清晰具体，学生能直接照做
3. 必须包含自检规则（check_rule），让学生自己判断是否完成
4. 可选给一个简短示例

请以 JSON 格式输出，结构如下：
{{
  "tasks": [
    {{
      "title": "微任务标题",
      "instruction": "详细的任务说明",
      "check_rule": "自检规则：学生如何判断自己是否完成了这个任务",
      "example": "简短示例（可选）",
      "time_estimate": "预计耗时"
    }}
  ]
}}
